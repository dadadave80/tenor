// Tailwind v4 is installed for future screens; the landing page is authored with the design's own
// tokens and inline styles, so nothing here rewrites its CSS.
export default { plugins: { '@tailwindcss/postcss': {} } }
